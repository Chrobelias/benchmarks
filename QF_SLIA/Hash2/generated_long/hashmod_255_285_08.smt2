(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "924") (re.opt (re.++ (str.to_re "333") (re.+ (re.union (re.range "7" "8") (re.range "7" "9"))))) (str.to_re "664641") (re.+ (re.union (re.range "2" "6") (str.to_re "862"))) (str.to_re "557415"))))
(assert (= (mod (mod (str.to_int x) 281) 51) 32))
(assert (> (str.len x) 100))
(check-sat)
(get-model)