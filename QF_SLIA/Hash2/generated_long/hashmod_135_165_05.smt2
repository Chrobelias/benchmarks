(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "9") (re.+ (re.union (re.range "0" "3") (str.to_re "66"))) (re.opt (re.++ (str.to_re "56829") (re.* (re.range "6" "8")))))))
(assert (= (mod (mod (str.to_int x) 151) 43) 21))
(assert (> (str.len x) 100))
(check-sat)
(get-model)