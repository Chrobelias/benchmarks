(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.* (re.++ (str.to_re "11") (re.* (re.range "2" "9")))) (re.* (re.++ (str.to_re "36475") (re.opt (re.range "6" "9")))) (re.opt (re.union (re.range "2" "4") (str.to_re "151515"))) (re.opt (str.to_re "407682")))))
(assert (= (mod (mod (str.to_int x) 16) 14) 13))
(assert (> (str.len x) 100))
(check-sat)
(get-model)