(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.opt (re.union (re.range "3" "9") (str.to_re "5678"))) (re.opt (str.to_re "567")))))
(assert (= (mod (mod (str.to_int x) 46) 41) 22))
(assert (> (str.len x) 100))
(check-sat)
(get-model)