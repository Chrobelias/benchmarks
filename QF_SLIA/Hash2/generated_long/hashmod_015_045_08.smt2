(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.opt (re.++ (str.to_re "444") (re.* (re.range "2" "9")))) (re.* (re.union (re.union (re.range "7" "8") (re.range "3" "5")) (str.to_re "7979"))))))
(assert (= (mod (mod (str.to_int x) 18) 7) 2))
(assert (> (str.len x) 100))
(check-sat)
(get-model)