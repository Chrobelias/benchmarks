(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.+ (re.++ (str.to_re "962717") (re.* (re.range "1" "3")))) (re.* (re.++ (str.to_re "73") (re.+ (re.range "6" "9")))) (str.to_re "580574"))))
(assert (= (mod (mod (str.to_int x) 232) 86) 49))
(assert (> (str.len x) 100))
(check-sat)
(get-model)