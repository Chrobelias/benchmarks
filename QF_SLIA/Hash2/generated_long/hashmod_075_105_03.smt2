(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "25") (re.opt (re.union (re.range "0" "6") (str.to_re "9999"))))))
(assert (= (mod (mod (str.to_int x) 81) 48) 43))
(assert (> (str.len x) 100))
(check-sat)
(get-model)