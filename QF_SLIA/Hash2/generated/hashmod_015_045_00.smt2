(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "4444") (re.opt (re.union (re.range "2" "4") (str.to_re "66666"))) (re.opt (str.to_re "1234")))))
(assert (= (mod (mod (str.to_int x) 18) 5) 5))
(assert (< (str.len x) 100))
(check-sat)
(get-model)