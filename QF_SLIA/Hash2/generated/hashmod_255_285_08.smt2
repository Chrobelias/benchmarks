(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "666") (re.* (re.range "3" "9")))))
(assert (= (mod (mod (str.to_int x) 280) 144) 142))
(assert (< (str.len x) 100))
(check-sat)
(get-model)