(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "70") (re.opt (re.++ (str.to_re "343086") (re.* (re.range "6" "9")))))))
(assert (= (mod (mod (str.to_int x) 283) 273) 263))
(assert (< (str.len x) 100))
(check-sat)
(get-model)