(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.* (re.union (re.range "0" "7") (str.to_re "549952"))) (re.opt (re.union (re.range "0" "1") (str.to_re "135379"))))))
(assert (= (mod (mod (str.to_int x) 172) 171) 129))
(assert (> (str.len x) 100))
(check-sat)
(get-model)