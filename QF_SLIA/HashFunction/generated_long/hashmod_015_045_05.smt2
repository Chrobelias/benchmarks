(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "3456") (re.+ (re.union (re.range "2" "8") (str.to_re "888"))) (re.opt (re.union (re.range "3" "5") (re.range "7" "8"))))))
(assert (= (mod (mod (str.to_int x) 33) 7) 0))
(assert (> (str.len x) 100))
(check-sat)
(get-model)