(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "999") (re.opt (re.range "0" "3")))))
(assert (= (mod (mod (str.to_int x) 123) 60) 16))
(assert (< (str.len x) 100))
(check-sat)
(get-model)