(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "7967988") (re.opt (re.++ (str.to_re "40311") (re.* (re.range "7" "9")))))))
(assert (= (mod (mod (str.to_int x) 24) 17) 11))
(assert (< (str.len x) 100))
(check-sat)
(get-model)